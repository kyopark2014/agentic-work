# Vendored third-party notice

`scripts/vendor/drawio-jsapi` and `scripts/lib/layoutGuidance.js` /
`scripts/lib/layoutPatterns.js` are derived from:

https://github.com/aws-samples/sample-drawio-mcp

License: MIT-0 (see upstream LICENSE)

The standalone CLI (`drawio_cli.mjs`) wraps DiagramEngine for agent skills
without requiring the MCP server runtime.
